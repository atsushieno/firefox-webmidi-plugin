RT-WebMIDI: Web MIDI API support for Firefox.

Nothing is implemented. It is just a stub yet.
No required JavaScript is written, nor C++ plugin implementation.

Build
-----

Makefile is for xpi packaging.

scons is for building plugin native library (libwebmidi.so).

